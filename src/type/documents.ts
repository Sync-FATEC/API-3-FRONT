type documents = {
    removed:boolean
    documentId: string;
    fileName: string;
    fileType: string;
    fileUrl: string;
    uploadedAt: string | null;
    user: string | null;
}

export default documents